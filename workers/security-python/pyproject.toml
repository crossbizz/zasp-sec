[project]
name = "zasp-security-worker"
version = "0.0.0"
requires-python = ">=3.13,<3.14"
dependencies = []

[project.scripts]
security-worker = "security_worker.__main__:cli"

[tool.setuptools]
packages = ["security_worker"]
