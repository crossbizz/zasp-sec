# Explicit migration51 command

The rendered schema51 job called up-to-51, but the executable accepted only
earlier targets. Scripted command RED reproduced errInvalidMigrationCommand;
the built executable PostgreSQL RED installed50 then rejected51 in6.627s.

The explicit command now dispatches guarded UpProductionRuntimePrecision after
50 and checks compiled51 readiness after existing principal registration.
Plain up remains49. Historical up/down commands refuse51 and no downgrade
command was added. Missing principal configuration rejects before migration.
Idempotent51 invocation verifies readiness; drift/future/mixed requests refuse.

Implementer focused actualPG races passed14.259s, including compiled executable
coverage and historical50 controls. Root reviewed the scoped production delta
and independently ran both precision command tests against PG18, PASS9.738s.
The full command suite first found three historical unknown-command sentinels
still rejecting51; those now reject52 without changing earlier target behavior.
The full package then passed77.613s. Root's requested fixture correction removes
inherited PG-prefixed variables; the final test also uses the existing owned
process supervisor. Final built-binary actualPG races passed10.002s after that
helper change. The full package run compiled just before the process-helper-only
adjustment; the final focused run covers that adjustment. Root inspected the
final harness and production delta with no remaining scoped findings.

No live migration, release approval or original task publication credit.
