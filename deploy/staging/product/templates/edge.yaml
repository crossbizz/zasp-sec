{{- if eq .Values.profile "customer_edge" }}
{{- fail "customer_edge is not shipped: publish the exact executable image, entrypoint, security review, and deployed proof before enabling this profile" }}
{{- end }}
